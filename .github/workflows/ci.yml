name: Stack-Agnostic CI

on:
  push:
    branches: ["main"]      
  pull_request:
    branches: ["main"]

# 1. PERMISSIONS (Fixes the 403 Forbidden error)
permissions:
  contents: read
  packages: read
  statuses: write

jobs:
  prepare-matrix:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.read-config.outputs.matrix }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - id: read-config
        name: Read config.json and extract app IDs
        shell: bash
        run: |
          echo "🔍 Reading config.json..."
          cat config.json || echo "❌ config.json not found!"

          # Extract keys under .apps (["source_1","source_2",...])
          MATRIX=$(jq -c '.apps | keys' config.json)

          echo "🔍 Extracted matrix: $MATRIX"
          echo "matrix=$MATRIX" >> $GITHUB_OUTPUT

  build-and-test:
    needs: prepare-matrix
    runs-on: ubuntu-latest

    strategy:
      matrix:
        app-id: ${{ fromJson(needs.prepare-matrix.outputs.matrix) }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup build environment for ${{ matrix.app-id }}
        id: setup
        uses: ./.github/actions/setup
        with:
          app-id: ${{ matrix.app-id }}
          config-path: ./config.json

      ######################################################################
      #                             DYNAMIC LINTING                        #
      ######################################################################

      - name: Prepare lint paths
        id: lintprep
        shell: bash
        run: |
          echo "Lint enabled: ${{ steps.setup.outputs.lint }}"
          echo "Lint path raw JSON: ${{ steps.setup.outputs.lint_path }}"

          if [ "${{ steps.setup.outputs.lint }}" != "true" ]; then
            echo "LINT_TARGETS=" >> $GITHUB_ENV
            exit 0
          fi

          LINT_PATH_JSON='${{ steps.setup.outputs.lint_path }}'

          # Case 1: null → lint entire directory
          if [ "$LINT_PATH_JSON" = "null" ]; then
            DIR="${{ steps.setup.outputs.directory }}"
            echo "LINT_TARGETS=(${DIR}/.*)" >> $GITHUB_ENV
            exit 0
          fi

          # Case 2: string/object → convert to list
          TARGETS=$(echo "$LINT_PATH_JSON" | jq -r '
            if type=="string" then
              [.]
            else
              [.[]]
            end | join(" ")
          ')

          # Build Regex Pattern for Super-Linter
          REGEX_PARTS=()
          APP_DIR="${{ steps.setup.outputs.directory }}"

          for t in $TARGETS; do
            t_trimmed=$(echo "$t" | xargs)
            [ -z "$t_trimmed" ] && continue
            
            if [[ "$t_trimmed" == /* ]]; then
               CLEAN_PATH="${t_trimmed#/}"
            else
               CLEAN_PATH="${APP_DIR}/${t_trimmed}"
            fi

            if [[ "$CLEAN_PATH" == *.* ]]; then
               REGEX_PARTS+=("${CLEAN_PATH}")
            else
               REGEX_PARTS+=("${CLEAN_PATH}/.*")
            fi
          done

          IFS="|"
          REGEX_STRING="(${REGEX_PARTS[*]})"
          IFS=" " # Reset IFS

          echo "LINT_TARGETS=$REGEX_STRING" >> $GITHUB_ENV
          echo "Computed LINT_TARGETS (Regex): $REGEX_STRING"

      - name: Run Super-Linter
        if: env.LINT_TARGETS != ''
        uses: super-linter/super-linter@v6
        env:
          DEFAULT_BRANCH: "main"
          FILTER_REGEX_INCLUDE: ${{ env.LINT_TARGETS }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # 2. DISABLE ERRORS (Allows pipeline to continue even if linting fails)
          DISABLE_ERRORS: true

      ######################################################################
      #                             DOCKER BUILD                           #
      ######################################################################

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Docker Hub
        if: github.event_name != 'pull_request'
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
        run: |
          echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

      - name: Build & Push Image
        if: github.event_name != 'pull_request'
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          APP_DIR: ${{ steps.setup.outputs.directory }}
          COMMIT_SHA: ${{ github.sha }}
        run: |
          echo "📝 DEBUG: APP_DIR='$APP_DIR'"
          
          IMAGE_APP=$(basename "$APP_DIR")
          echo "📝 DEBUG: IMAGE_APP='$IMAGE_APP'"

          if [ -z "$APP_DIR" ]; then
            echo "❌ ERROR: APP_DIR is empty. Composite action did not output directory."
            exit 1
          fi

          docker build -t $DOCKERHUB_USERNAME/$IMAGE_APP:latest "$APP_DIR"
          docker push $DOCKERHUB_USERNAME/$IMAGE_APP:latest
          docker tag $DOCKERHUB_USERNAME/$IMAGE_APP:latest \
                      $DOCKERHUB_USERNAME/$IMAGE_APP:$COMMIT_SHA
          docker push $DOCKERHUB_USERNAME/$IMAGE_APP:$COMMIT_SHA